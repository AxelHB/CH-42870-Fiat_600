SI NODE, LES CREA UNA CARPETA DE NODE, CREAMOS EN LA RAIZ UN ".git_ignore" y dentro escribimos
la siguiente linea /node_modules.
IGNORAMOS TAMBIEN EL PACKAGE.JSON DENTRO DEL ".git_ignore" con la siguiente linea /package.json

-TECLA CONTROL +D selecciona varios elementos.

Al presionar CTRL+Z para deshacer y CTRL+Y para rehacer.

Por defecto para comentar es: ctrl + k + c.

git push origin main -f